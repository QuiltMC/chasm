{
    string: "Test",
    integer: 1234,
    boolean: true,
    empty: none,
    map: {
        string: "Inner"
    },
    list: [
        "first",
        "second",
        "third"
    ],
    operation: 4 + 2,
    function: argument -> argument * 2,
    call: root.function(4),
    call2: root.function(5),
    curry: arg1 -> arg2 -> arg1 - arg2,
    call_curry: root.curry(4)(3),
    ternary: 5 > 3 ? "larger" : "not",
    fibonacci: n -> n < 3 ? 1 : root.fibonacci(n - 1) + root.fibonacci(n - 2),
    fib_10: root.fibonacci(10),
    compare: 5 > 3 + 1,
    compare2: 5 > 3 + 3
}
